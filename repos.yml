default:
  repos_to_index: 100
  orgs:
    # GitHub Organisations of interest
    - ansible
  repos:
    - ansible/ansible
  blacklist:
    # use the pin name
    - 'ansible|ansible'
  collections:
    - awx.awx
